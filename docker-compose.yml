

services:
  db:
    image: postgres:15
    container_name: my_postgres
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_USER: devuser
      POSTGRES_PASSWORD: devpass
      POSTGRES_DB: devdb
    volumes:
      - devburguer_pgdata_v2:/var/lib/postgresql/data

  backend:
    build: .
    container_name: my_backend
    ports:
      - "3001:3001"
    volumes:
      - .:/app
    depends_on:
      - db

volumes:
  devburguer_pgdata_v2:
